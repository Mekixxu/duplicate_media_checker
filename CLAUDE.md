# CLAUDE.md

## 文档目的

本文件用于给LLM agent 提供完整上下文，帮助用户完成家庭影音与相册服务从 C 机部分迁移到 E 机的工作。

请把本文件视为任务说明书，而不是泛泛的技术建议文档。
目标是继续推进落地，不要重复讨论已经被用户明确否决的路线。

## 用户当前硬件与角色

- A：Win11 小主机，接 4K 电视，负责本地观影，MadVR 跑在 2060S 上。
- B：Win11 工作/游戏主机，9950X3D + 4070S，不是本次迁移重点。
- C：Win10 LTSC，12100，2x2T SSD，18T + 20T HDD，64GB RAM。
- C 当前承担：
  - 家庭影视与照片主存储。
  - Windows Docker Desktop 上运行 `Immich`。
  - `Jellyfin` 服务。
  - 一个 Win10 虚拟机，用于迅雷/百度网盘下载高风险 NSFW 内容。
  - 已安装卡巴斯基全功能版，用户重视安全性。
- D：腾讯云主机，当前用于 `ZeroTier Moon` 与 `WireGuard`。
- E：闲置小主机，AMD Ryzen 3 PRO 4150GE，16GB RAM，256GB SSD。
  - 现在已经安装好 `Ubuntu Server`。
  - 已接入内网。
  - 已安装 `Docker`。
  - 已安装 `v2raya`，用于稳定访问 Google/GitHub。

## 用户核心需求

- 外出时：
  - 在手机/电脑上访问家里的 `Immich` 与 `Jellyfin`。
  - 希望尽量走点对点打洞，避免 D 机转发导致 5M 带宽成为瓶颈。
  - 如果打洞成功，希望远程也能更顺畅地同步照片。
- 在家时：
  - A\&B 能稳定访问 C 上的影视库。
  - 手机照片继续备份。
- 服务目标：
  - `Immich` 更稳定。
  - `Jellyfin`远程看高码率视频时，尽可能具备服务端转码能力。
  - 避免 C 因虚拟网卡/容器环境不稳定而重启。

## 已确认的问题

- 用户当前外网访问主要依赖 `WireGuard` 路径：外部设备先连到 D，再由 D 通过隧道转发到 C。
- 因此远程访问的实际瓶颈不是家里内网，而是 D 的公网带宽；D 当前仅有 5M 带宽。
- 这会直接限制远程 `Jellyfin` 与 `Immich` 体验，例如 `Jellyfin` 往往只能压到约 4Mbps / 720p 才能较流畅播放。
- C 上运行 `ZeroTier` 时，会不定时重启。
- 用户查看过 Windows Event Viewer，错误来源与网卡相关。
- 关闭 `ZeroTier` 后，C 重启问题消失。
- Windows Docker Desktop 运行 `Immich` 不稳定，用户已经多次遇到卡死问题。
- C 的整机功耗约 40W-60W，用户认为偏高。

## 已确定的架构方向

- **C 保留为主存储节点**。
- **E 作为 Linux 服务节点与网络旁路节点**。
- `Immich` 与可能的 `Jellyfin` 优先迁移到 E 上运行。
- `ZeroTier` 也优先迁移到 E 上运行，而不是继续留在 C 上。
- D 继续作为 `Moon` 节点存在。

此方案的本质是：

- 把不稳定因素从 C 上剥离出去。
- 让 C 专注于存储和 Windows 环境下必须保留的任务。
- 让 E 承担 Linux 更适合做的网络与容器职责。

## 已明确排除的路线

以下路线已经讨论过，**不要再反复建议**，除非用户自己再次主动提起：

- 不把 `IPv6` 作为主方案。
  - 原因：用户明确说明 IPv6 极不稳定，且跨运营商体验受限。
- 不把 `Tailscale` 作为主方案。
  - 原因：用户已经实测，在中国大陆环境中，`Tailscale` 的打洞效果不满意。
  - 用户认为 `ZeroTier + Moon` 的成功率和性能明显更好。
- 不建议在 C 的 `Hyper-V` 里长期跑 Linux 虚拟机作为主方案。
  - 原因：会重新把网络和服务寄生回 C。
  - 用户更需要物理隔离和稳定性。
  - 该路线还可能让 GPU/转码与网络路径更复杂。
- 不把 `Unraid` 作为当前执行方案。

## 已明确的系统选择

- E 机选用 `Ubuntu Server`。

## 迁移目标的优先级

请按以下优先级推进，不要一开始追求“大而全”：

1. 先保证 E 上基础环境稳定。
2. 再把 `ZeroTier + Moon` 跑在 E 上，验证远程访问链路。
3. 再迁移 `Immich`。
4. 最后视情况决定 `Jellyfin` 是否也迁移到 E。

重点说明：

- `Immich` 迁移到 E 是高优先级，因为 Windows Docker Desktop 已被证明不稳定。
- `Jellyfin` 是否迁到 E，需要先看 E 上对 AMD 4150GE 核显转码的实际可用性与性能。
- 如果 E 的转码不理想，`Jellyfin` 可以暂时留在 C，等 `Immich` 迁移稳定后再评估。

## 关于 Jellyfin 转码的现实约束

用户非常关心远程播放高码率视频，例如 100Mbps 级别素材。

请注意以下原则：

- 不要空口保证“E 一定能轻松转任意 4K 高码率片源”。
- 正确做法是：
  - 在 E 上先验证 AMD 4150GE 的 `/dev/dri` 是否正常暴露。
  - 在 `Jellyfin` 中测试 VA-API 硬件解码/编码是否可用。
  - 用一两个真实高码率样本做实际转码测试，再给结论。
- 如果 E 上转码效果一般，也不要硬迁所有媒体服务。
- 服务迁移的核心目标首先是稳定性，其次才是极限转码性能。

## 推荐的实际落地顺序

### 第一阶段：盘点与保底

- 记录 C 上当前 `Immich`、`Jellyfin`、`ZeroTier`、`WireGuard` 的配置状态。
- 备份 `Immich` 数据库和关键配置。
- 明确 C 上照片目录、媒体目录、下载目录的实际路径。
- 明确哪些目录必须保留在 C 上，不允许直接迁移数据。

### 第二阶段：E 上基础能力确认

- 确认 `Ubuntu Server` 已正常更新。
- 确认 Docker 服务可用。
- 保留 `v2raya`，但避免它干扰内网访问和容器网络。
- 确认 E 拥有固定内网 IP，最好在主路由器里做 DHCP 绑定。
- 确认 E 可访问 C 的 SMB 共享，或准备通过 NFS/SMB 暴露给 E。

### 第三阶段：先迁 ZeroTier

- 在 E 上安装 `ZeroTier`。
- 加入用户现有网络。
- 绑定并验证 `Moon` 节点。
- 如果需要旁路由/子网路由，按 Linux 路由方式配置。
- 优先验证“外网设备 -> ZeroTier -> E -> C 上服务”是否可通。

关键要求：

- 新链路稳定前，不要立刻拆掉 C 上现有可用的远程访问方案。
- 应先并行验证，再切换主用路径。

### 第四阶段：迁移 Immich

- 在 E 上用原生 Docker / Docker Compose 部署 `Immich`。
- 用户照片原始数据应尽量继续放在 C 上，由 E 通过网络共享挂载访问。
- 如果数据库也迁移，必须先做好备份，再导入。
- 迁移后先验证：
  - 已有照片是否完整可见。
  - 新照片上传是否稳定。
  - 缩略图、元数据、人脸识别任务是否正常运行。

### 第五阶段：评估 Jellyfin 是否迁移

- 如果 E 上 `/dev/dri` 正常，尝试在容器中映射给 `Jellyfin`。
- 在 `Jellyfin` 后台配置硬件加速后做样本测试。
- 优先测试以下场景：
  - 远程限码率播放。
  - 高码率 HEVC 文件转码。
  - 手机客户端直连与转码切换。
- 如果结果理想，再迁移 `Jellyfin` 到 E。
- 如果结果不理想，可保留 `Jellyfin` 在 C，仅把 `Immich` 迁到 E。

## 对另一位 agent 的工作要求

请遵守这些约束：

- 不要忽视用户的已知实测结论。
- 不要再默认推荐 `Tailscale`、`IPv6` 作为主方案。
- 不要把 `Hyper-V Linux on C` 当成首选架构。
- 不要要求用户重装 C 为 `Unraid` 或其他 NAS 系统，除非用户重新改变目标。
- 不要为了“理论最优”而破坏当前已有可用链路。
- 优先做小步快跑、可回退、能验证的迁移。

## 用户真实关心的不是“理论最优”，而是这些

- 远程访问速度是否明显变快。
- `Immich` 是否比现在稳定。
- C 是否不再因为 `ZeroTier` 而重启。
- 高码率视频远程是否比现在更可用。
- 整体维护成本是否下降。

你给出的每一步建议，都最好能回答这 5 个问题中的至少一个。

## 建议优先产出的交付物

如果你要继续协助用户，请优先产出以下内容，而不是继续泛泛讨论：

1. E 上 `ZeroTier + Moon + 路由转发` 的可执行步骤。
2. E 上 `Immich` 的 `docker compose` 部署模板。
3. C 到 E 的 SMB 挂载方案。
4. `Jellyfin` 在 E 上启用 `/dev/dri` 与 VA-API 的验证步骤。
5. 迁移验收清单与回退方案。

## 验收标准

当迁移基本完成时，至少应满足以下条件：

- E 稳定在线，可访问 Google/GitHub，同时不影响内网服务。
- 外网设备能通过 `ZeroTier + Moon` 稳定访问 E 和/或 C。
- C 不再需要运行导致重启的 `ZeroTier` 组件。
- `Immich` 在 E 上运行稳定，且照片库可正常访问。
- 用户可明确判断：
  - 远程访问体验是否优于原先 5M 中转方案。
  - 是否继续推进 `Jellyfin` 一并迁移。

## 如果信息不足，应优先询问的问题

在动手前，如果你需要补全现场信息，优先问这些问题：

- C 上影视库和照片库的具体路径是什么？
- C 是否已经开启 SMB 共享？共享账号权限如何？
- D 的 Moon 配置是否已经成熟可复用？
- 用户外网主要通过手机、笔记本，还是两者都要？
- 目前 `Jellyfin` 是直接读本地盘，还是已经走网络共享？
- E 与 C 是否在同一交换机/路由下，当前内网速率是 1GbE 还是更高？

## 最后提醒

本任务不是“重新设计整个家庭网络”，而是：

- 在用户现有成功经验基础上做迁移。
- 最大化复用用户已验证可行的 `ZeroTier + Moon` 路线。
- 用 E 机承接 Linux 更擅长的职责。
- 逐步把 C 从不稳定因素中解放出来。

如果你需要在多个方案中做选择，请优先选择：

- 更少中断。
- 更容易回退。
- 更贴合用户实测经验。
- 更能快速验证收益的方案。

