# Tasks
- [x] Task 1: 修改按钮文案
  - [x] SubTask 1.1: 在 `movie_manager/templates/report.html` 中，将“全局删除选中文件”替换为“删除全部选中文件”。
  - [x] SubTask 1.2: 在 `movie_report.html` 中，将“全局删除选中文件”替换为“删除全部选中文件”。
- [x] Task 2: 扩大复选框点击区域
  - [x] SubTask 2.1: 在 `movie_manager/templates/report.html` 的 `<style>` 中，添加针对 `.el-checkbox` 的样式，利用 padding、margin 或 scale 扩大其点击热区（例如让 `.el-table .el-checkbox` 更大）。
  - [x] SubTask 2.2: 在 `movie_report.html` 的 `<style>` 中，应用相同的样式调整。